import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({
	item : {
		padding : ".5rem",
	},
	element : {
		padding : "0 0 0 1rem",
		width   : "fit-content",
	},
}));

export default useStyles;
